import React from "react";
import Modal from "../../../Copmonents/ui/Modal";

const ContactModal = ({ isOpen, setIsOpen, _id }) => {
  return <Modal isOpen={isOpen} setIsOpen={setIsOpen}></Modal>;
};

export default ContactModal;
